.dsagent/
__pycache__/
*.egg-info/
.pytest_cache/
.venv/
.env
.env.*

# The Claude desktop app drops a copy here when a screenshot is sent to chat.
# Never repo content — anything worth keeping is committed under docs/runs/.
Claude outputs/

# ui/ — ui/.env is committed on purpose: it holds no secrets, only the
# telemetry opt-out and the default agent URL.
!ui/.env
ui/node_modules/
ui/.next/
ui/next-env.d.ts
ui/tsconfig.tsbuildinfo
.DS_Store
