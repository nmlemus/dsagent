---
name: ds-eda-to-report
description: Run the "eda-to-report" workflow. Profile a dataset, run the data quality gate, and deliver a findings report. Two personas, kernel env only — the harness smoke test.
---
<!-- AUTO-GENERATED by dsagent from workflows/eda-to-report/workflow.yaml — do not edit. -->

# /ds-eda-to-report

Profile a dataset, run the data quality gate, and deliver a findings report. Two personas, kernel env only — the harness smoke test.

**Inputs**

- `data_path` (path)
- `question` (string)
- `key_column` (string)

**Steps**

1. **profile** — marie
2. **data-gate** — marie · gate: human
3. **analyze** — noel
4. **report** — marie

**How to run**

In DSAgent, call the `run_workflow` tool with `name="eda-to-report"` and the inputs above.
Personas allowed to start it: marie, noel.

In Claude Code (no DSAgent backend), execute the steps in order yourself, delegating
each one to the named persona subagent and reading its instructions from
`workflows/eda-to-report/steps/`. Stop at every human gate and ask the user.
