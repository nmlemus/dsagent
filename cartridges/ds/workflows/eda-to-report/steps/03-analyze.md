Answer this question with the data: **{question}**

Read `artifacts/data-profile.md` and `artifacts/data-gate.md` first so you know
what to trust. Use `run_python` (state persists) for the analysis. Produce up to
five figures under `artifacts/figures/` following the chart rules in the
`reports` skill.

Write `artifacts/findings.md`: three headline findings, then evidence per finding
(chart reference + numbers with denominators), then caveats that come from the
gate. No modeling in this step — descriptive and comparative analysis only.
