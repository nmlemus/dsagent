Evaluate the data gate for the modeling table using the `eda` skill thresholds plus
the MMM-specific checks: no missing periods per geo, every media channel has both an
impressions and a spend column, KPI has no negative values, at least 2 years of
weekly data (or state the shortfall).

Write `artifacts/data-gate.md`, ending with `GATE: PASS` or `GATE: FAIL — <reason>`.
