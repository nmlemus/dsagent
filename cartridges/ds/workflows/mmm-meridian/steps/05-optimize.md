Load `model/posterior.pkl` and run the analysis and optimization stages from the
`mmm` skill.

Produce `artifacts/budget-optimization.md` with: contribution and ROI per channel
with credible intervals; response curves saved under `artifacts/figures/`; and at
least two budget scenarios (same total budget re-allocated; total +10 %) with the
expected incremental KPI and its interval for each. State the constraints you used.
