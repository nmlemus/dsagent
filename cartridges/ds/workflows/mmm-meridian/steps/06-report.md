Write the stakeholder report for this MMM using the `reports` skill structure,
drawing on `artifacts/model-spec.md`, `artifacts/diagnostics.md` and
`artifacts/budget-optimization.md`.

Headline findings must be budget decisions, not model facts. Include the data-gate
caveats. Write `report/mmm-report.md` and render `report/mmm-report.html` with the
`reports` skill script.
