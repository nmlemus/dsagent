"""DSAgent v2 — a thin harness on top of Deep Agents that loads cartridges.

The harness is domain-agnostic. Everything domain-specific (personas, skills,
workflows, execution environments) lives in a cartridge directory that is also
a valid Claude Code plugin.
"""

__version__ = "2.0.0a1"
