@AGENTS.md

The repository root `CLAUDE.md` still applies in here — read it first. Nothing in
this directory relaxes its invariants; in particular, `ui/` is a **view**. It
renders what `dsagent serve` sends and never decides what a persona does, which
step runs next, or what a workflow is. Agent behaviour belongs in
`cartridges/ds/`, workflow structure in `workflow.yaml`, and the event and gate
contracts in `docs/ui-slice.md` §3.

`AGENTS.md` above is generated by `next dev` and only describes Next.js itself.
